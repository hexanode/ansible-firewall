---

- name: Disable UFW firewall
  systemd:
    name: ufw
    state: stopped
    enabled: false
  when: "'ufw.service' in services"

- name: Disable Netfilter firewall
  systemd:
    name: nftables
    state: stopped
    enabled: false
  when: "'nftables.service' in services"
